<settings>
    <servers>
        <server>
            <id>ossrh</id>
            <username>${env.MAVEN_PUBLISHER_USER}</username>
            <password>${env.MAVEN_PUBLISHER_PASSWORD}</password>
        </server>
    </servers>
</settings>
